Create a DAILY AWS Advanced Networking study session aligned to the AWS Certified Advanced Networking – Specialty (ANS-C01) content outline. The purpose is deep operational and design understanding of AWS and hybrid network architectures at scale.

CERTIFICATION-STATUS GOVERNANCE — HARD REQUIREMENT: at the beginning of every run, check the official AWS certification page and record the certification status and check date. Do not imply that ANS-C01 is available to schedule when the official page says it has retired or its last testing date has passed. If it is retired, label the report `ANS-C01 blueprint-based advanced AWS networking study` and retain the content as practical professional-development material. Use the official exam guide as the blueprint authority:
`https://docs.aws.amazon.com/aws-certification/latest/advanced-networking-specialty-01/advanced-networking-specialty-01.html`
Use the official certification-status page as the status authority:
`https://aws.amazon.com/certification/certified-advanced-networking-specialty/`

DAILY CADENCE — HARD REQUIREMENT: run every day at the scheduled time.

EXAM-BLUEPRINT WEIGHTING — HARD REQUIREMENT: align the daily report and its 50 questions to the official ANS-C01 domains and weights:
1) Network Design — 30% (15 questions)
2) Network Implementation — 26% (13 questions)
3) Network Management and Operation — 20% (10 questions)
4) Network Security, Compliance, and Governance — 24% (12 questions)
The daily 50-question distribution must total exactly 15/13/10/12. A technology can appear in more than one domain only when the question explicitly tests a different domain skill.

MANDATORY AWS TECHNOLOGY / SOLUTION-FAMILY COVERAGE — HARD REQUIREMENT: EVERY SINGLE RUN MUST include substantive coverage for EACH AND EVERY one of these twelve targets. Keep the focus on services and features in the current official ANS-C01 in-scope-service list and exam guide. Where a service has been superseded, newly introduced, or is not listed as in scope, identify it as contextual only and do not let it displace an in-scope feature.

1) Amazon VPC foundations: CIDR and subnet design, route tables, internet/NAT/egress-only gateways, IPv4/IPv6 behavior, ENIs, and VPC addressing choices.
2) Hybrid connectivity: AWS Direct Connect, Site-to-Site VPN, customer/VPN gateways, BGP, routing preference, redundancy, and failover.
3) Transit and multi-VPC networking: AWS Transit Gateway attachments, associations, propagations, route domains, peering, RAM sharing, and centralized connectivity design.
4) DNS and service discovery: Amazon Route 53 routing, Resolver, private hosted zones, health checks, and AWS Cloud Map where applicable.
5) Private service access: AWS PrivateLink, interface/gateway endpoints, endpoint services, DNS integration, endpoint policies, and cross-account access.
6) Load balancing and service insertion: Application Load Balancer, Network Load Balancer, Gateway Load Balancer, target health, listeners, routing, and symmetric service-chain design.
7) Edge and global traffic delivery: Amazon CloudFront, AWS Global Accelerator, Route 53 traffic-routing policies, origin reachability, and regional failover design.
8) Network inspection and perimeter protection: AWS Network Firewall, Gateway Load Balancer service insertion, AWS WAF, AWS Shield, egress filtering, and ingress-defense placement.
9) Network access control and governance: security groups, network ACLs, VPC endpoint policies, IAM/RAM implications, AWS Firewall Manager, and account/organization boundaries.
10) Network observability and operations: VPC Flow Logs, Amazon CloudWatch, AWS CloudTrail, AWS Config, route/attachment state, Reachability Analyzer or equivalent AWS network-analysis tools when officially supported.
11) Deployment and automation: AWS CloudFormation, AWS CLI/API/SDK workflows, idempotent network changes, dependency ordering, change safety, and multi-account governance automation.
12) Workload and service integration: EC2, Auto Scaling, Amazon ECS/EKS, AWS Lambda, Amazon API Gateway, and Amazon S3 network access patterns that materially affect AWS network design.

TARGET-MAPPING REQUIREMENT: every target must visibly map its feature to one or more official domains. The report must include a coverage matrix that proves all twelve targets are present and that the 50-question domain distribution is 15/13/10/12.

FEATURE-FIRST PRIMARY CONTENT — HARD REQUIREMENT AND OVERRIDING SELECTION RULE: the PRIMARY lesson for every mandatory target MUST be based on an AWS feature-, architecture-, configuration-, deployment-, administration-, design-, CLI/API-, automation-, or service-guide source that teaches how the capability works. Do NOT select an AWS re:Post answer, troubleshooting article, support incident, defect note, blog post about a symptom, or remediation KB as the primary lesson source unless no feature-oriented AWS source exists for that capability. In that exceptional case, mark the selection as a validation failure and choose another feature instead.

The central purpose of every lesson is to teach the FEATURE itself: what problem it solves; architecture; components; route, DNS, health, policy, and control-plane behavior; configuration objects; order of operations; control/management/data-plane behavior; dependencies; normal packet/session/event flow; supported modes; feature interactions; limits; commands/Console/API/IaC workflows; operational evidence; design tradeoffs; scale, HA, regional, and multi-account considerations; and how an engineer should reason about it.

PRIMARY FEATURE SOURCE TYPES TO PREFER: AWS Documentation service guides; AWS Prescriptive Guidance; AWS Architecture Center; AWS Well-Architected Framework and lenses; official whitepapers; AWS CLI/API/CloudFormation references; official AWS workshops; AWS networking blogs when they explain a feature from first principles; release-specific documentation; and official architecture examples. AWS re:Post or Knowledge Center material is acceptable as scenario support, not normally as the primary feature lesson.

FEATURE SOURCE VS SCENARIO SOURCE SEPARATION — HARD REQUIREMENT: every target must visibly show two distinct source roles:
1) `Primary feature lesson source:` exact official AWS feature/configuration/design source used to teach the capability.
2) `Scenario support source(s):` one or more AWS re:Post, AWS Knowledge Center, AWS Support public documentation, AWS troubleshooting guide, official AWS blog incident/troubleshooting example, or exact AWS documentation source showing a concrete failure pattern related to that same capability.
The primary lesson source and scenario support source must normally be different URLs. Do not let a troubleshooting article become the article being taught merely because it contains a useful symptom.

SUPPORT SOURCES ARE FOR APPLICATION, NOT THE LESSON — HARD REQUIREMENT: AWS re:Post, Knowledge Center, Support, and troubleshooting material must be used to construct realistic production scenarios that REQUIRE application of the mechanics learned from the primary feature lesson. A quiz question must NOT simply ask the learner to remember the exact fix, text, identifier, workaround, or conclusion from the support source. Transform the support case into a distinct engineering scenario that tests the underlying route, policy, DNS, health-check, endpoint, BGP, state, dependency, or failure-boundary behavior. Preserve source-supported technical conditions but change the decision being tested so the learner must reason from the feature model.

FEATURE-CENTRIC DAILY ROTATION — HARD REQUIREMENT: for each mandatory target, choose one substantive feature/capability per run and rotate across the broader AWS networking knowledge domain. Favor capabilities with architecture and configuration depth. Balance the daily mix across the four official domains rather than over-focusing on VPC routing alone.

PRIMARY CONTENT-SELECTION RULE — UNIQUENESS FIRST: every day's FEATURE, primary feature source, support scenario sources, workflow, evidence, scenario, CLI/API/IaC command or configuration fragment, remediation path, topology, and quiz pattern must be materially different from every run in the rolling previous 30 calendar days. Reuse becomes eligible only after 30 days and is still optional. Prefer uncovered in-scope feature material first. A new re:Post or support case does not make a repeated feature lesson unique if the underlying lesson is substantially the same.

ARTICLE DEPTH — COMPREHENSIVE SELF-CONTAINED FEATURE LESSON: every primary feature source becomes a detailed original engineering lesson. Begin with a concise `Essence / mental model`, then comprehensively teach what the feature is; why it exists; architecture/components; prerequisites/dependencies; normal route, DNS, control, and data flow; configuration hierarchy and object relationships; policy/order-of-operations logic; Console/CLI/API/CloudFormation workflows; relevant commands and fields; expected/healthy state; state transitions; feature interactions; failure boundaries; scale/HA/Region/AZ/upgrade implications; quotas and version/platform caveats; design tradeoffs; common engineering mistakes; and key takeaways. The learner should understand the feature without clicking the source. Never reproduce copyrighted source material verbatim.

DEPTH EXPECTATION: substantial feature lessons should normally be roughly 1,000–2,500+ words when the source supports that depth. Narrow features may be shorter, but must still explain the mechanisms required to reason about an advanced scenario.

SOURCE TRACEABILITY — HARD REQUIREMENT FOR EACH TARGET: visibly show `Primary feature lesson source:`, exact title, complete raw `https://...` URL, service/platform/Region/release when available, source classification, source/update date when available, and one sentence stating exactly what feature behavior that source supports. Separately show `Scenario support source(s):` with exact title, raw URL, classification/date, and one sentence stating which failure pattern or operational condition each source supports. Identify whether the scenario is an official AWS document, re:Post, Knowledge Center, public support guidance, or official AWS blog.

EVIDENCE OUTPUT + INTERPRETATION: when the primary feature or scenario source publishes useful CLI output, route-table state, CloudFormation/JSON/YAML syntax, Console state, VPC Flow Log records, CloudWatch metrics, CloudTrail events, BGP information, Network Firewall state, DNS lookup results, health status, or configuration fields, include concise source-backed evidence and then explicitly state `What this proves`, `What this does not prove`, and `Next discriminating check`. Never invent output. If a source documents syntax but not canonical output, say so.

REALISTIC SCENARIOS IN LESSONS — HARD REQUIREMENT: each mandatory target must include at least one real-world troubleshooting or design scenario derived from AWS-supported material but explicitly tied back to the feature model. Include topology/role, Region/AZ/account context where relevant, exact symptom, service/version/platform context where relevant, evidence, what remained healthy, competing hypotheses, discriminating check, root-cause class, remediation, post-fix verification, and `Feature principle being tested` explaining which AWS network mechanic made the diagnosis possible.

MERMAID DIAGRAMS — NEW HARD REQUIREMENT WHERE APPLICABLE: add Mermaid diagrams directly inside the canonical HTML for lessons where a visual materially improves understanding. Prefer Mermaid for hybrid routing and BGP failover, VPC route selection, Transit Gateway route domains, PrivateLink DNS/endpoint flow, GWLB/Network Firewall inspection paths, load-balancer data flow, Route 53/Global Accelerator failover, security-policy order, VPC Flow Logs/CloudTrail/CloudWatch evidence flow, CloudFormation dependency ordering, and workload-to-service network paths. Do NOT force Mermaid into lessons where a diagram would add little value. Aim for meaningful Mermaid coverage across the report, normally at least 6 of the 12 targets when technically appropriate.

MERMAID IMPLEMENTATION RULES: render Mermaid source inline in the HTML using normal `<pre class="mermaid">...</pre>` or equivalent containers. Use one pinned Mermaid JavaScript version from a stable CDN, initialize it once on page load, and ensure the report remains readable if Mermaid fails to load by keeping the Mermaid source text visible or providing a concise adjacent textual fallback. Do not base64-embed Mermaid JavaScript. Do not generate diagrams by fetching external diagram services. Keep diagrams responsive, avoid horizontal overflow, use short node labels, and prefer simple flowchart/sequence/state diagrams over overly dense art. All diagram text must be original synthesis, not copied verbatim from AWS figures. Never represent uncertain behavior as fact.

MERMAID SOURCE TRACEABILITY: each Mermaid diagram must include a visible caption such as `Mermaid teaching diagram — synthesized from the AWS feature model` plus the exact primary feature source URL(s) that support the depicted relationships. When a scenario-specific state transition is included, also cite the relevant scenario support URL. Add a `What to notice:` note explaining the diagram. Clearly label the diagram as a teaching synthesis rather than an official AWS figure.

MERMAID VS ORIGINAL AWS IMAGES: Mermaid does NOT replace the existing technical-image discovery requirement. Continue to inspect primary feature sources for useful original AWS-hosted JPG/PNG/WebP figures. When both are useful, include the original source figure and a separate Mermaid teaching diagram. Original source figures must retain their existing provenance and white figure-card rules.

MERMAID VALIDATION — HARD FAIL IF USED: verify that every Mermaid block has syntactically valid Mermaid text, a visible caption, source traceability, no invented nodes/flows unsupported by the lesson, responsive styling, and a textual fallback. Verify Mermaid initialization JavaScript exists exactly once. Record `Mermaid diagrams present: N`, `Mermaid syntax validation: PASS`, and `Mermaid source-traceability: PASS` in the report validation section and in `docs/index.html` latest-run summary.

AWS TROUBLESHOOTING PRIORITY FOR QUIZ SCENARIOS — HARD REQUIREMENT: at least 80% of quiz questions must be realistic production troubleshooting/design scenarios grounded in AWS Documentation, AWS re:Post, AWS Knowledge Center, public AWS Support guidance, AWS Prescriptive Guidance, or official AWS architecture/troubleshooting examples with concrete operational conditions. The questions must test the FEATURE lesson, not recall of the support article.

PROHIBITED QUESTION STYLE — HARD FAIL: no generic shells; no questions asking for a support-case title, exact error text, document identifier, or memorized workaround; no repeated `which command did AWS recommend?`; no direct restatement of a source article's conclusion; no leaked AWS certification-exam questions or exam-dump content.

QUESTION ROOT-CAUSE AND ACTION DIVERSITY — HARD REQUIREMENT: within each target, 3+ questions must normally cover different failure boundaries/root-cause classes and different correct actions. Across the report, vary routing, DNS, security policy, service state, health checks, multi-account access, BGP, IPv4/IPv6, automation dependency, observability, availability-zone, and cost/scale tradeoffs.

TECHNICAL IMAGE / FIGURE DISCOVERY — HARD REQUIREMENT: actively inspect each accepted PRIMARY FEATURE SOURCE for useful architecture diagrams, topology figures, Console screenshots, CLI/API screenshots, packet/route-flow figures, policy diagrams, workflows, charts, or technical tables rendered as images. Give preference to feature-source figures because they help teach architecture. Scenario-source images may be included only when they materially clarify failure evidence.

EXTERNALLY LINKED IMAGES ONLY: never embed image bytes/base64/blob/SVG/XML/local image files. If a verified AWS-hosted JPG/PNG/WebP exists, use normal external `<img src="https://...">` with a white figure canvas, responsive sizing, alt text, technical caption, `What to notice:`, visible raw `Original image URL:`, and visible raw `Source article URL:`. If no verified useful image exists, omit the figure block silently. Never fabricate image URLs or display missing-image fallback notices. Do not treat AWS architecture icons as an official technical figure unless the image is actually present in the cited source.

QUIZ — EXACTLY 50 AUTOMATICALLY GRADABLE QUESTIONS: exactly 50 questions covering ONLY that day's feature lessons, all twelve mandatory targets represented with at least 3 questions each, and the domain distribution exactly 15 Network Design / 13 Network Implementation / 10 Network Management and Operation / 12 Network Security, Compliance, and Governance. Use a realistic mix of single-select, multiple-response, and matching questions. For multiple-response and matching questions, make the required selections/matches explicit and grade all required choices together. Maintain balanced A/B/C/D positions for single-select items, 50 answer-check controls, final grading, answered/unanswered and checked/unchecked status, an 80% PASS threshold, and domain/target performance reporting.

QUIZ DIFFICULTY — ADVANCED AWS NETWORK ARCHITECT/ENGINEERING: questions must require reasoning from feature architecture, object dependencies, normal state, control/management/data-plane relationships, route/DNS/policy order of operations, and source-backed evidence. At least 40/50 must be scenario-based; >=30/50 grounded in official AWS support/troubleshooting or implementation cases; >=20 multi-step reasoning; >=15 competing-hypothesis; >=10 configuration/design; >=8 cross-service or cross-account integrations. Repeated evidence excerpts: 0. Duplicate question fingerprints: 0. Predictable/template-cloned families: 0. Per-target repeated correct-action pattern: 0. Per-target root-cause diversity: PASS. Distractor plausibility: PASS. Answer-length/specificity giveaway: PASS.

SELF-CONTAINMENT FOR EVIDENCE QUESTIONS — HARD FAIL: if a question depends on evidence, include that exact evidence inside the same question card with service/topic/Region or account context, command/source context, evidence, `Primary feature lesson source`, `Scenario support source`, and complete visible raw URLs. Feedback must explain what the evidence proves, what it does not prove, why the best answer follows from the AWS feature mechanics, why each distractor is plausible but wrong/premature, best next check, and `What feature principle you missed`.

MANDATORY FEATURE-FIRST VALIDATION — HARD FAIL: `Primary lessons based on feature/configuration/architecture sources: 12/12`; `Primary lessons based on troubleshooting/defect-only sources: 0/12`; `Separate scenario support sources present: 12/12`; `Questions testing feature mechanics rather than support-article recall: PASS`; `Support article conclusion copied as quiz answer pattern: 0`.

MANDATORY ARTICLE-DEPTH VALIDATION: for each target verify the lesson covers feature purpose, production relevance, architecture/components, dependencies, normal state/flow, configuration/policy logic, Console/CLI/API/IaC workflow, evidence/fields, interpretation, feature interactions, failure boundaries, troubleshooting application, remediation, verification, design implications, Region/AZ/account/platform caveats, common mistakes, takeaways, source title, and visible raw URL. Shallow support summaries fail.

MANDATORY 30-DAY UNIQUENESS VALIDATION: compare against previous 30 days and require history checked YES; targets 12/12; fresh primary feature URLs 12/12; duplicate feature/lesson/scenario/primary-source/command-evidence/evidence-excerpt/question fingerprints all 0; 30-day uniqueness PASS; overall Validation PASS.

MACHINE-READABLE HISTORY: embed timestamp; certification-status check result/source URL; official-domain mapping; twelve targets; feature/topic fingerprints; primary-feature-source fingerprints; scenario-support-source fingerprints; failure/scenario/CLI/API/IaC/evidence/remediation/question/distractor/answer-position/image fingerprints; Mermaid diagram fingerprints/source URLs when present; source-case identifier; root-cause class; correct-action class; difficulty/reasoning type; and last-used dates.

HTML REPORT: one polished responsive HTML containing executive summary, current certification-status notice, exact ANS-C01 domain distribution, twelve deep feature-first lessons, source-role separation, realistic AWS troubleshooting/design scenarios applying those features, evidence interpretation, Console/CLI/API/CloudFormation reference, verified external technical figures, Mermaid teaching diagrams where applicable, source index, exactly 50 advanced questions with detailed rationales, final grading, feature-first validation, mandatory-target validation, domain-distribution validation, article-depth validation, image-link validation, Mermaid validation, quiz difficulty/root-cause/answer-diversity validation, 30-day uniqueness validation, and valid history JSON.

INDEX.HTML — TOP SUMMARY HARD REQUIREMENT: update `docs/index.html` so the latest validated run is first. Show timestamp, certification-status check and official status link, report link, mandatory targets covered 12/12, all target names, each day's feature topic, each target's official ANS-C01 domain mapping, primary feature source article + visible raw source URL, separate scenario-support source counts/links, technical image URLs only when verified, Mermaid diagram count/validation when present, quiz validation, exact 15/13/10/12 domain distribution, feature-first validation, article-depth validation, 30-day uniqueness PASS, and a prominent Open latest report link. Preserve prior runs below.

GITHUB PUBLICATION — CONFIGURATION REQUIRED: publish only after the destination repository and GitHub Pages base URL are confirmed. Default configuration, if this is the intended repository:
Repository: `ccaiccie4/AWS-Networking-Specialty`
Branch: `main`
Report directory: `docs/`
Report filename: `AWS_Advanced_Networking_Specialty_Daily_Study_Quiz_YYYY-MM-DD-HH-MM.html`
Index: `docs/index.html`
GitHub Pages URL pattern: `https://ccaiccie4.github.io/AWS-Networking-Specialty/<filename>.html`
If the repository or Pages URL differs, update these two configuration values before enabling publication. Never substitute a different project repository or URL. Fetch the report and index back after commit and rerun certification-status, mandatory-target, domain-distribution, feature-first, article-depth, source-role separation, evidence, image-link, Mermaid, quiz difficulty/root-cause/answer-diversity, 30-day uniqueness, and index validations. If any validation fails, do not email.

GITHUB PAGES URL — PRIMARY DELIVERY URL: use the confirmed Pages report URL as the primary user-facing report URL. Do not claim a URL is published until the committed report is reachable and passes the fetch-back validation.

EMAIL DELIVERY — HARD REQUIREMENT: only after committed report/index pass validation, send an HTML email from the connected Gmail account to BOTH configured recipients. Subject: `AWS Advanced Networking Specialty Daily Study Quiz — YYYY-MM-DD-HH-MM`. Include the exact GitHub Pages link as the primary button and a visible copyable URL; all twelve targets and feature topics; ANS-C01 domain distribution; primary feature-source summary; scenario-support summary; article-depth summary; scenario/difficulty validation; image-link summary; Mermaid diagram summary/count; feature-first validation; certification-status result; and 30-day uniqueness status. Verify Gmail reports SENT to both recipients before claiming success.
